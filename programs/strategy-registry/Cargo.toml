[package]
name = "strategy-registry"
version = "0.1.0"
description = "Minimal StratIn strategy commitment registry"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "strategy_registry"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "1.2.0"
